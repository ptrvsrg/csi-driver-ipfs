# Copyright 2026 ptrvsrg.
#
# Licensed under the Apache License, Version 2.0 (the License);
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an 'AS IS' BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

version: 2

project_name: csi-driver-ipfs

# Git tags are driver/v*; CI sets GORELEASER_CURRENT_TAG=v… so semver and {{ .Version }} work.
before:
  hooks:
    - go mod download

builds:
  - id: csi-driver-ipfs
    main: ./cmd/csi-driver-ipfs
    binary: csi-driver-ipfs
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.driverVersion={{.Version}}
      - -X main.gitCommit={{.ShortCommit}}
      - -X main.buildDate={{.Date}}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
      - goos: darwin
        goarch: arm64
        goarm: 7

archives:
  - id: default
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}'

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc

dockers:
  - image_templates:
      - 'ghcr.io/ptrvsrg/csi-driver-ipfs:{{ .Version }}-amd64'
      - "ghcr.io/ptrvsrg/csi-driver-ipfs:latest-amd64"
    dockerfile: Dockerfile
    use: buildx
    build_flag_templates:
      - '--build-arg=VERSION={{ .Version }}'
      - "--build-arg=GIT_COMMIT={{ .FullCommit }}"
      - "--build-arg=BUILD_DATE={{ .Date }}"
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title=csi-driver-ipfs"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - '--label=org.opencontainers.image.version={{ .Version }}'
    goos: linux
    goarch: amd64
  - image_templates:
      - 'ghcr.io/ptrvsrg/csi-driver-ipfs:{{ .Version }}-arm64'
      - "ghcr.io/ptrvsrg/csi-driver-ipfs:latest-arm64"
    dockerfile: Dockerfile
    use: buildx
    build_flag_templates:
      - '--build-arg=VERSION={{ .Version }}'
      - "--build-arg=GIT_COMMIT={{ .FullCommit }}"
      - "--build-arg=BUILD_DATE={{ .Date }}"
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title=csi-driver-ipfs"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - '--label=org.opencontainers.image.version={{ .Version }}'
    goos: linux
    goarch: arm64

docker_manifests:
  - name_template: 'ghcr.io/ptrvsrg/csi-driver-ipfs:{{ .Version }}'
    image_templates:
      - 'ghcr.io/ptrvsrg/csi-driver-ipfs:{{ .Version }}-amd64'
      - 'ghcr.io/ptrvsrg/csi-driver-ipfs:{{ .Version }}-arm64'
  - name_template: "ghcr.io/ptrvsrg/csi-driver-ipfs:latest"
    image_templates:
      - "ghcr.io/ptrvsrg/csi-driver-ipfs:latest-amd64"
      - "ghcr.io/ptrvsrg/csi-driver-ipfs:latest-arm64"

release:
  github:
    owner: ptrvsrg
    name: csi-driver-ipfs
  draft: false
  mode: replace
