apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ipfs-cluster.scriptsConfigMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ipfs-cluster.labels" . | nindent 4 }}
data:
  entrypoint.sh: |
    #!/bin/sh
    set -e
    if [ ! -f /data/ipfs-cluster/service.json ]; then
      ipfs-cluster-service init
    fi
    PEER_HOSTNAME=$(cat /proc/sys/kernel/hostname)
    if echo "$PEER_HOSTNAME" | grep -q "ipfs-cluster-0"; then
      if [ -n "${BOOTSTRAP_PEER_PRIV_KEY}" ]; then
        CLUSTER_ID="${BOOTSTRAP_PEER_ID}" \
        CLUSTER_PRIVATEKEY="${BOOTSTRAP_PEER_PRIV_KEY}" \
        exec ipfs-cluster-service daemon --upgrade
      else
        exec ipfs-cluster-service daemon --upgrade
      fi
    else
      BOOTSTRAP_ADDR="/dns4/{{ include "ipfs-cluster.fullname" . }}-0.{{ include "ipfs-cluster.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local/tcp/9096/ipfs/${BOOTSTRAP_PEER_ID}"
      exec ipfs-cluster-service daemon --upgrade --bootstrap "$BOOTSTRAP_ADDR" --leave
    fi
  bootstrap-peer-id: {{ .Values.bootstrapPeerId | quote }}
  configure-ipfs.sh: |
    #!/bin/sh
    set -e
    mkdir -p /data/ipfs
    if [ -f /data/ipfs/config ]; then
      if [ -f /data/ipfs/repo.lock ]; then
        rm -f /data/ipfs/repo.lock
      fi
      chown -R 1000:1000 /data/ipfs 2>/dev/null || true
      exit 0
    fi
    ipfs init --profile=flatfs,server
    ipfs config Addresses.API /ip4/0.0.0.0/tcp/5001
    ipfs config Addresses.Gateway /ip4/0.0.0.0/tcp/8080
    ipfs config --json Swarm.ConnMgr.HighWater 2000
    ipfs config --json Datastore.BloomFilterSize 1048576
    ipfs config Datastore.StorageMax 100GB
    chown -R 1000:1000 /data/ipfs 2>/dev/null || true
